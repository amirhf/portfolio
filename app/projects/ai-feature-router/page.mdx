export const metadata = { title: 'AI Feature Router — Case Study' }
 
# AI Feature Router — Local‑first captioning & search
**Win:** Latency/cost‑aware routing: run CLIP/BLIP locally, call cloud when thresholds demand it.
## Approach
FastAPI + workers; embeddings in Qdrant/pgvector; semantic search endpoint; metrics UI.
## Next
A/B policy tuner and batch evaluation.
